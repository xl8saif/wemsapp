# WEMS online production environment
WEMS_ENV=production
SECRET_KEY=replace-with-a-long-random-secret
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME
# Optional: persistent writable data directory. Render uses /var/data.
WEMS_DATA_DIR=/var/data
